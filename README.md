# render-demo

A tiny Node/Express web app created and deployed to [Render](https://render.com) through the Render REST API.

- `GET /` — landing page
- `GET /api/info` — runtime/deploy info as JSON
- `GET /healthz` — health check

## Run locally
```bash
npm install
npm start
```
